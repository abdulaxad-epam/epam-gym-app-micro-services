spring:
  application:
    name: epam-gym-app-eureka-server

server:
  port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    serviceUrl:
      defaultZone: http://eureka-service:8761/eureka/
  instance:
    hostname: eureka-service
